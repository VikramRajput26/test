@{
    ViewData["Title"] = "Dashboard";
}

<h2>Dashboard</h2>

<p>
    <a asp-action="AddStudent" class="btn btn-primary">Add Student</a>
    <a asp-action="GetStudents" class="btn btn-primary">View Students</a>
    <a asp-action="Delete" class="btn btn-primary">Delete Student</a>
    <a asp-action="GetById" class="btn btn-primary">Get Student By ID</a>
    <a asp-action="Update" class="btn btn-primary">Update Student</a>
    <a asp-action="SearchByStatus" class="btn btn-primary"> SearchByStatus</a>
    <a asp-action="SortByStatus" class="btn btn-primary">SortByStatus</a>
</p>
